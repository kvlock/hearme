# HearMe Sign Language App

Sign Language Detection App Using Python, TensorFlow and OpenCV

---

## 🛠 Installation

To get this project running locally, follow these steps:

1. **Clone the repository:**
   ```bash
   git clone https://github.com/kvlock/hearme.git
   cd hearme

2. **Install dependencies:**
   ```bash
   pip install -r requirements.txt

3. **💻Usage**
   ```bash
   python main.py
4.  **📂Project Structure**
* `main.py` - The entry point of the application.
* `requirements.txt` - A list of the Python dependencies.
* `utils/` - Folder containing helper functions.
* `ui/` -Folder cointaining the UI files.
* `models/` -Folder containing the models.
* `core/` -Core files folder.
